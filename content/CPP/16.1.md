---
title: "Exercise 16.1"
categories: CPP
---
```c
#include <math.h>
#include <stdio.h>
#define MAX(X,Y) ((X)>(Y)?(X):(Y))
#define MIN(X,Y) ((X)<(Y)?(X):(Y))
#define PI acos(-1)
#define EATLINE() while (getchar() != '\n') continue;
#define IS_EVEN(X) ((X) % 2 == 0)
#define IS_ODD(X) ((X) % 2)
```
